# Terminal Server WebSocket URL
NEXT_PUBLIC_TERMINAL_WS_URL=ws://localhost:8080

# Repository path to mount in terminal container
# Defaults to ./workspace if not set
REPO_PATH=/path/to/your/repository
